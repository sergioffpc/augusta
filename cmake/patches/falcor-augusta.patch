diff --git a/CMakePresets.json b/CMakePresets.json
index 8d52e7b1..cfd264a2 100644
--- a/CMakePresets.json
+++ b/CMakePresets.json
@@ -96,6 +96,18 @@
                 "windows-ninja-msvc-base"
             ]
         },
+        {
+            "name": "windows-ninja-msvc-augusta",
+            "displayName": "Windows Ninja/MSVC (augusta)",
+            "description": "augusta's own vendoring of this submodule (see augusta's ADR-0009): pins policy compatibility for the vendored pybind11 copy against newer CMake, and exports compile_commands.json so augusta's own build can mirror this target's exact compile flags.",
+            "inherits": [
+                "windows-ninja-msvc-base"
+            ],
+            "cacheVariables": {
+                "CMAKE_POLICY_VERSION_MINIMUM": "3.5",
+                "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
+            }
+        },
         {
             "name": "windows-ninja-msvc-ci",
             "displayName": "Windows Ninja/MSVC (CI)",
@@ -178,6 +190,18 @@
             "configurePreset": "windows-ninja-msvc",
             "configuration": "Debug"
         },
+        {
+            "name": "windows-ninja-msvc-augusta-release",
+            "displayName": "Release",
+            "configurePreset": "windows-ninja-msvc-augusta",
+            "configuration": "Release"
+        },
+        {
+            "name": "windows-ninja-msvc-augusta-debug",
+            "displayName": "Debug",
+            "configurePreset": "windows-ninja-msvc-augusta",
+            "configuration": "Debug"
+        },
         {
             "name": "windows-ninja-msvc-ci-release",
             "displayName": "Release",
diff --git a/Source/Falcor/Utils/Timing/ProfilerUI.cpp b/Source/Falcor/Utils/Timing/ProfilerUI.cpp
index 0daa8513..4154be73 100644
--- a/Source/Falcor/Utils/Timing/ProfilerUI.cpp
+++ b/Source/Falcor/Utils/Timing/ProfilerUI.cpp
@@ -50,26 +50,29 @@ const float kHeaderSpacing = 5.f;
 
 const size_t kHistoryCapacity = 256;
 
-// Colorblind friendly palette.
+// "Luminance Ramp" (augusta): one green hue family, alternating
+// dark/light steps so neighboring events in the table/graph always
+// differ sharply in brightness - picked over a hue-spread or mixed
+// scheme for being the most distinguishable at a glance.
 const std::vector<uint32_t> kColorPalette = {
-    IM_COL32(0x00, 0x49, 0x49, 0xff),
-    IM_COL32(0x00, 0x92, 0x92, 0xff),
-    IM_COL32(0xff, 0x6d, 0xb6, 0xff),
-    IM_COL32(0xff, 0xb6, 0xdb, 0xff),
-    IM_COL32(0x49, 0x00, 0x92, 0xff),
-    IM_COL32(0x00, 0x6d, 0xdb, 0xff),
-    IM_COL32(0xb6, 0x6d, 0xff, 0xff),
-    IM_COL32(0x6d, 0xb6, 0xff, 0xff),
-    IM_COL32(0xb6, 0xdb, 0xff, 0xff),
-    IM_COL32(0x92, 0x00, 0x00, 0xff),
-    // Yellow-ish colors don't work well with the highlight color.
-    // IM_COL32(0x92, 0x49, 0x00, 0xff),
-    // IM_COL32(0xdb, 0x6d, 0x00, 0xff),
-    IM_COL32(0x24, 0xff, 0x24, 0xff),
-    IM_COL32(0xff, 0xff, 0x6d, 0xff),
+    IM_COL32(0x0a, 0x2e, 0x0a, 0xff),
+    IM_COL32(0xa8, 0xff, 0x7a, 0xff),
+    IM_COL32(0x1f, 0x5c, 0x1f, 0xff),
+    IM_COL32(0xd9, 0xff, 0xc2, 0xff),
+    IM_COL32(0x2e, 0x8b, 0x3d, 0xff),
+    IM_COL32(0x7f, 0xff, 0x9f, 0xff),
+    IM_COL32(0x12, 0x3b, 0x1a, 0xff),
+    IM_COL32(0xc2, 0xff, 0xb3, 0xff),
+    IM_COL32(0x3f, 0xaf, 0x5a, 0xff),
+    IM_COL32(0xf0, 0xff, 0xe0, 0xff),
 };
 
-const uint32_t kHighlightColor = IM_COL32(0xff, 0x7f, 0x00, 0xcf);
+// Magenta (augusta) - was orange upstream, then light green; magenta is
+// green's complement on the color wheel, so it stays clearly readable as
+// "the selected one" against every shade in the all-green kColorPalette
+// regardless of that shade's own brightness, instead of blending in
+// (light green did, being just another point on the same ramp).
+const uint32_t kHighlightColor = IM_COL32(0xff, 0x2f, 0xd0, 0xcf);
 
 void drawRectFilled(const ImVec2& pos, const ImVec2& size, uint32_t color = 0xffffffff)
 {
diff --git a/Source/Falcor/Utils/Timing/ProfilerUI.h b/Source/Falcor/Utils/Timing/ProfilerUI.h
index 16f6f4f2..99c70caa 100644
--- a/Source/Falcor/Utils/Timing/ProfilerUI.h
+++ b/Source/Falcor/Utils/Timing/ProfilerUI.h
@@ -96,8 +96,8 @@ private:
 
     Profiler* mpProfiler; ///< Profiler instance.
 
-    GraphMode mGraphMode = GraphMode::Off; ///< Graph mode.
-    bool mEnableAverage = true;            ///< Use averaged time values (EMA).
+    GraphMode mGraphMode = GraphMode::CpuTime; ///< Graph mode.
+    bool mEnableAverage = false;               ///< Use averaged time values (EMA).
 
     struct EventData
     {
